class Solution(object):
    def removeStars(self, s):
        res=[]
        for char in s:
            if char=='*':
                res.pop()
            else:
                res.append(char)    
        return "".join(res)        
        
